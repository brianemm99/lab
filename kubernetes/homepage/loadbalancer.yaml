apiVersion: v1
kind: Service
metadata:
  labels:
    app: homepage
  name: homepage-loadbalancer
  namespace: homepage
spec:
 ports:
  - port: 3001
    protocol: TCP
    targetPort: 3000
 selector:
   app.kubernetes.io/instance: my-release
   app.kubernetes.io/name: my-release
 type: LoadBalancer
